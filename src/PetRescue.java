import view.GUI;
import view.UI;

public class PetRescue {
    public static void main(String[] args) {
    	/** The only thing we have to do is to create whether the
    	 *  GUI or UI constructor and call his function
    	 */
    	GUI graphicalInterface = new GUI();
    	UI textInterface = new UI();    	
    }
}
